# سجلّ التغييرات — خادم منصّة مركز تمكين الكفاءات

كل إضافة تُقيَّد هنا قبل وسمها. المدخل يجيب أربعة أسئلة: **ما أُضيف**،
**ما لم يُمَسّ**، **ما يلزم لتشغيله**، و**كيف يُرجَع عنه**.

الأحدث أولاً.

---

## ٢٠٢٦-٠٨-٢٢ — حرّاس موجة الجدولة: القفل يُطبَّق، وتعارض الوقت يُمنع

**الوسم:** `checkpoint/wave-guards-v1` *(يُوسَم بعد التحقّق)*

### ما أُضيف

مراجعةٌ لموجة الجدولة كشفت أن قواعدها **معلنةٌ ولا تُنفَّذ**. سبعة حرّاس:

**١) قفل الموجة صار موضعاً واحداً.** كان `periodError()` دالّةً خاصّة مكرّرة
حرفياً في `ScheduleController` و`DiscussionCircleController`، تُستدعى في خمسة
مواضع من أحد عشر تكتب في موجة. صارت `App\Services\WaveGuard` تُحقن وتُنادى في
المواضع كلّها — ومنها ثلاثةٌ كانت مكشوفة: حذفُ الجلسة، وحذفُ الحلقة وسحبُ
مشاركٍ منها (وهو حذفُ صفٍّ فعليّ)، وحذفُ الموجة نفسها.

**ودرجتا تجميد لا واحدة:** ما تُبنى منه الموجة (`BUILD`) يُقفل عند الاعتماد،
وما يُبنى بعده (`FOLLOW_UP` — الجدول الذهبي وتأشير الخطوات) لا يُقفل إلا عند
الإغلاق. خطوات الإجراء من السادسة إلى الثانية عشرة كلّها لاحقةٌ للاعتماد،
فتجميدُها عنده يجعل قائمة الإجراء غير قابلة للإكمال.

**٢) جلسة التعويض لا تدخل موجةً معتمَدة.** كان `reschedule()` يورّث الموجة متى
وقع التاريخ الجديد داخل مداها — والغياب لا يُسجَّل إلا وموجةٌ معتمَدة تعمل، فكانت
كلُّ جلسة تعويضٍ تُضاف إلى موجةٍ ختمها مدير المركز بصلاحية `candidate.edit`
وحدها، بلا رفضٍ ولا أثر. صارت تذهب إلى الموجة المفتوحة التي تشمل تاريخها، فإن
تعدّدت أو لم توجد بقيت بلا موجة حتى يُسنِدها المُجدوِل بيده.

**٣) تعارض الوقت يُمنع.** «النصاب عدّاد لا سدّ، وتعارض الوقت يُمنع» قرارٌ كان
مكتوباً في تعليقٍ وغيرَ مكتوبٍ في أي طبقة. أربعة فهارس فريدة جزئية: المقيّم
والمساعد في المقابلة، والمشارك في كل نشاط، والمساعد في حلقات النقاش (المستشار
كان محروساً منذ إنشاء الجدول والمساعد بجانبه بلا حارس). والقيدُ هو الحارس لا
فحصٌ في الكود تسبقه ضغطتان متزامنتان، و`WaveGuard::conflictMessage` يترجم 23505
إلى عربية.

**والجلسات الجماعية مستثناة**: أدوات القياس والتمرين التكاملي مشرفٌ واحد وعدّة
مشاركين في اللحظة نفسها، وحلقة النقاش حارسُها في جدولها. النصابُ يبقى عدّاداً.

**٤) من أرسل الموجة لا يعتمدها.** كان `CENTER_MANAGER` يحمل `schedule.manage`
و`schedule.approve_center` معاً فيبني ويرسل ويعتمد وحده — وخطوة «الإرسال
للاعتماد» بلا معنى تقني. الفحص على الشخص (`submitted_by`) لا على الدور، فمديرٌ
صحّح نصاباً في لوحة موجةٍ بناها غيره لا يفقد حقّه في اعتمادها.

**٥) لا يُسلَّم إلا المعتمَد.** كان التسليم للوزارة يُقبل من موجةٍ مسودّة أو
مغلقة، وشاشةُ التسليم تفتح على أحدث موجةٍ أياً كانت حالتها. والتسليم فعلٌ خارج
المنصّة يُبنى عليه حضور أناس.

**٦) قائمة مشرفي أدوات القياس كانت فارغةً أبداً.** `MEASURE_SUPER` ليس في
`User::SECTOR_BOUND_ROLES`، و`UserController` يرفض أن يُعطى قطاعاً — فـ`sector_id`
فيه `NULL` دائماً، والمساواةُ بالعمود تُسقطه. صار الشرط على **الدور** لا على
خلوّ العمود. والاختبارُ القائم كان يُخفي العطل: مساعده يكتب القطاع مباشرةً
متجاوزاً قاعدة المسار.

**٧) النصاب يُنذر من الخادم.** كان الإنذار شرطاً في قالب شاشةٍ واحدة، فسكتت عنه
نافذة الجدولة وهي موضع الاختيار الفعلي، وسقط عمّن نصابه صفر (السقف صفرٌ قيمةٌ
كاذبة في جافاسكربت). وصار السقف يُبنى على أيام الموجة التي فيها جلسات لا على
أيام تقويمها — كان ينتفخ بالجُمَع والسبوت فيبتلع التجاوز الحقيقي.

ومعها: `period.activities` كان وجوداً واحداً غير مقيَّد بنشاط (مقيّم مقابلاتٍ
واحد يُرضي الخطوة عن موجةٍ كلّها حلقات نقاش)، و`period.golden_synced` كان
يفحص أقلّ ممّا يكتبه `sync` (اشتقاقان مختلفان للرمز، صارا `codeFor()` واحدة)،
و`dispatch_authorities` نُقل إلى `KEEP` في `PlatformReset`: كان يُمحى بـ
`--with-reference` بلا بذرةٍ تُعيده ولا شاشةٍ تُنشئه، فتصير الخطوة الحادية عشرة
غير قابلة للاكتمال أبداً.

### ما لم يُمَسّ

**النصاب يبقى عدّاداً لا سدّاً** — لا حارس جديد يمنع تجاوزه، والاختبار
`test_over_quota_is_still_only_a_warning` يحرس ذلك. ولوحةُ الموجة تبقى ترتيباً
للأسماء لا قائمةً مغلقة. وتأشيرُ الخطوات اليدوية يبقى مفتوحاً على الموجة
المعتمَدة — وهو المقصود لا سهو. والجلسةُ بلا موجة تُنشأ وتُعدَّل كما كانت.
ومسارُ `reschedule` يبقى على `candidate.edit` كما قرّرته الشيفرة صراحةً
(«إعادة الجدولة قرار إداري لا تسجيل»). ولا صلاحية جديدة، ولا تغيير في مصفوفة
الأدوار.

### ما يلزم لتشغيله

`php artisan migrate` — هجرةٌ واحدة (`2026_08_22_000001`) تُنشئ أربعة فهارس.

**وهي تكشف قبل أن تُنشئ**: القاعدة حيّة، وفهرسٌ فريد يفشل إنشاؤه إن سبقه تعارضٌ
مخزَّن. فإن وُجد، تتوقّف الهجرة برسالةٍ عربية **تُسمّي الصفوف المتعارضة** بدل
`23505` عارياً. عالِج التعارضات ثم أعِدها.

وعلى تثبيتٍ كبير: أنشئ الفهارس يدوياً بـ`CREATE UNIQUE INDEX CONCURRENTLY` خارج
المهاجرة (لا تعمل داخل معاملة) ثم شغّلها — `IF NOT EXISTS` يجعلها بلا أثر.

### كيف يُرجَع عنه

`php artisan migrate:rollback` يُسقط الفهارس الأربعة، و`git revert` يعيد
الحرّاس السبعة. لا بيانات تُفقد في الحالتين: الهجرة تُنشئ فهارس ولا تكتب صفّاً،
والحرّاس يرفضون ولا يحذفون.

### التحقّق

`php artisan test` — ٩٩٤ اختباراً تمرّ (٣٦٦٨ تأكيداً)، منها `ScheduleConflictTest`
(٨ اختبارات: ما يُمنع، وما لا يُمنع) وخمسةٌ أُضيفت في `SchedulingPeriodTest`.

---

## ٢٠٢٦-٠٨-٢٠ — بحيرة تقارير كفاءات: قاعدةٌ منفصلة تتراكم فيها المخرجات

**الوسم:** `checkpoint/lake-v1` *(يُوسَم بعد التحقّق)*

### ما أُضيف

قاعدةُ بياناتٍ ثانية مستقلّة (`kafaat_lake`) تتراكم فيها مخرجاتُ المنصّة،
لتقرأها منصّةٌ أخرى مستقبلاً أو تُغذّي خادماً تالياً.

**المنصّة هي الناشر لا المنشور عليه.** لا تتّصل جهةٌ خارجية بقاعدة المنصّة
أبداً: بعد كل انتقالٍ ناجح لتقرير يُكتب ظرفٌ مُجمَّد في `report_lake_outbox`
على القاعدة الأساسية، ويُفرّغه أمرُ `kafaat:lake:ship` من المجدول إلى البحيرة.
النسخُ المنطقيّ والاتصالُ المباشر مرفوضان: الأول يشحن نصّاً مشفّراً بـ`APP_KEY`
لا يملكه أحد سوى خادم التطبيق، والثاني يُسقط طبقاتِ التصنيف والنطاق وحجب
الأسماء الأربع التي تعيش في PHP لا في القاعدة.

**ولماذا لقطةٌ لا نسخ؟** جسدُ التقرير غير موجودٍ في القاعدة أصلاً:
`final_reports` يحفظ الترويسة، وتفصيلُ الكفاءات يُعاد حسابُه عند كل عرضٍ من
`evaluation_scores × competencies` — و`weight`/`max_level`/`target_*` قابلةٌ
للتحرير من الشاشة. فالتقرير نفسُه يُنتج أرقاماً مختلفةً قبل التعديل وبعده.
تُجمَّد الحسبةُ لحظةَ الحدث ومعها بصماتُ الأبعاد التي حُسبت بها.

**مجهوليّة كاملة** (قرار المالك): لا اسم، ولا رقم هوية، ولا جوال ولا بريد ولا
سيرة. معرّفٌ بديل `person_ref` = HMAC-SHA256 بفلفلٍ لا يغادر خادم التطبيق —
و`national_id_hash` لا يُصدَّر إطلاقاً لأنه SHA-256 بلا ملح على فضاءٍ من عشر
خانات، أي مكشوفٌ بالبحث الشامل. النصّ الحرّ ورمزُ المشارك معطّلان افتراضياً
خلف مفاتيح صريحة، والسردُ خلف دور قراءةٍ منفصل.

**والمُصنَّف لا يغادر**: `secret`/`top_secret` يُحجبان قبل بناء الحمولة، وشريطُ
تعثّرٍ في القاعدة يرفض أيَّ صفٍّ يفلت. من ٣٨ تقريراً: ٢٨ تُصدَّر و١٠ تُحجب.

**البنية:** `raw` (هبوطٌ غيرُ قابلٍ للتعديل، مُجزّأ شهرياً) → `curated` (إسقاطٌ
يُهدم ويُعاد بـ`lake.replay()`) → `contract_v1` (٢١ عرضاً، الواجهةُ الوحيدة
لأيّ مستهلك). أربعةُ أدوار: مالكٌ للهجرات، كاتبٌ يُدرج ولا يحذف، قارئُ أرقام،
قارئُ سرد.

### ما لم يُمَسّ

سلوكُ المنصّة كلُّه. `ReportController` أُضيف إليه سبعةُ نداءات ولم يُحذف منه
سطرٌ واحد (`diff` = صفر أسطر محذوفة)، ولم يُنقل مُصيِّرٌ ولا أُعيد تغليفُ
الكتابة المشروطة التي تحرس سباق TOCTOU. `ScoringService` لم يُمَسّ —
الفجوة تُشتقّ في خدمة اللقطة من التفصيل المُفهرَس بمعرّف الكفاءة.
و`PlatformReset` صُنِّف فيه الجدول الجديد (حارسُه رفض النشر حتى صُنِّف — وهو
تصرّفٌ صحيح).

### ما يلزم لتشغيله

قاعدةٌ مُهيّأة بـ`deploy/lake/00_bootstrap.sql` ثم `01`–`06`، ومفاتيح
`LAKE_ENABLED` و`LAKE_PEPPER` و`DB_LAKE_*` في `.env`، و`KAFAAT_LAKE=1` في
`deploy.conf` ليُهاجِر النشرُ مخطّطَ البحيرة. التفاصيل في
`deploy/lake/README.md`، وعقدُ المستهلك في `docs/LAKE_CONTRACT_v1.md`.

### كيف يُرجَع عنه

`LAKE_ENABLED=false` وإعادة نشر: المُصدِّر يصير لا-عمليّة صرفة ولا يُكتب صفّ.
الصندوق يتراكم بلا ضرر ويُفرَّغ يوم يُعاد التفعيل. وللإزالة الكاملة:
`git revert` + `DROP DATABASE kafaat_lake` + إسقاط الأدوار الأربعة. قاعدةُ
المنصّة لا تتأثّر في الحالتين.

### التحقّق

`968` اختباراً تمرّ (زادت ٣٧). و`deploy/lake/verify.sh` يؤكّد ٢١ ضابطَ
امتيازٍ بأدوارها الحقيقية — منها أن القارئ لا يرى `raw` ولا `curated`، وأن
الكاتب لا يحذف ولا يُعيد البناء ولا يمحو، وأن الحذف الموجَّه إلى قسمٍ بعينه
مرفوضٌ كالموجَّه إلى الأب. ومسحُ ٢٥٩ كيلوبايت من حمولات البحيرة مقابل ١٣٣
اسماً و١٣٣ رقم هويةٍ حقيقيّاً: صفرُ تطابقات.

---

---

## ٢٠٢٦-٠٨-٢٠ — فحص تكرار الهوية قبل ملء النموذج

**الوسم:** `checkpoint/id-lookup` *(يُوسَم بعد التحقّق)*

### ما أُضيف

`POST /candidates/lookup`. كان المُدخِل يملأ النموذج كاملاً — هوية واسم وقطاع
ورتبة وسيرة — ثم يُردّ بـ«هذا المشارك مُضاف مسبقاً». الآن يُسأل الباب أوّلَ ما
يكتمل رقم الهوية.

**`POST` لا `GET`**: رقم الهوية في مسارٍ يُكتب في سجلّ nginx وسجلّ المتصفّح
وسجلّ الوسيط. ومخنوقٌ بالمعدّل لأنه سطحُ تعدادٍ بأرقام الهوية.

**والاستجابة مقتصرة عمداً:** «مسجَّل / غير مسجَّل» وتاريخ الإضافة لمن يملك
التعديل، ولا رمزَ ولا اسمَ لمن لا يملكه. وإلا صار النموذج المساعد حاصدةَ رموزٍ
بتجريب الأرقام. واختبارات `CandidateLookupTest` مكتوبة على ما **لا** يُردّ بقدر
ما هي على ما يُردّ — فإضافةُ حقلٍ «مفيد» غداً تُسقِطها بدل أن تمرّ بصمت.

### ومعه

- **الخروج يمسح `sessionStorage`**: مسودّات النماذج تحمل هويةً واسماً وسيرة،
  وشاشات المركز مشتركة — فبقاؤها بعد الخروج يتركها لمن يجلس بعده.
- **حقول السيرة تتنفّس**: كانت ضيّقة على نصٍّ عربي طويل.
- **`moitp.gov.sa`** في nginx ودليل TLS، والعنوان يبقى عاملاً ريثما يعمّ DNS.

### التحقّق

`924` اختباراً تمرّ (زادت ثمانية).

> تُشغَّل على قاعدة اختبارٍ خاصّة عند فتح أكثر من جلسة على المشروع:
> `DB_DATABASE=kafaat_test_solo php artisan test`.

### كيف يُرجَع عنه

```bash
git revert <sha>      # لا هجرة — إضافةُ مسارٍ وشيفرةِ واجهة
```

## ٢٠٢٦-٠٨-٢٣ — ملفّ المشارك كما في نموذج المركز، والسيرة تصير إلزامية

**الوسم:** `checkpoint/participant-profile` *(يُوسَم بعد التحقّق)*

نموذج المركز المعتمد تسعةٌ وسبعون عموداً. المنصّة كانت تقرأ منه سبعة، والباقي
يُعاد إدخاله بيدٍ أو يضيع. الآن يُقرأ كلّه.

### ما أُضيف

**جدول `technical_areas` + `candidate_technical_areas`** — المجالات الفنية التي
يُرشَّح عليها المشارك، مبذورةً بأربعة ومُدارةً من الإعدادات. **جدولٌ لا أربعة
أعمدة منطقية**: التصنيف قرارُ مركزٍ لا ثابتُ شيفرة، ومجالٌ خامس صفٌّ يُضاف لا
هجرةٌ تُكتب. وهو غير `expertise_areas`: تلك تصف المقيّم بتخصّصه، وهذه تصف
المشارك بالجانب الذي يُقاس فيه.

**`candidates.gender`** عموداً — الجدولة تفلتر به وتعدّه، والوثيقة المشفّرة لا
تُفلتَر ولا تُفهرَس. قابلٌ للإفراغ في القاعدة ومطلوبٌ في التحقّق: الصفوف
القائمة سبقت العمود، ولا افتراضيَّ صحيحاً في سجلٍّ رسمي.

**وثيقة السيرة وُسِّعت** بخمسة حقول من النموذج: لقب الرتبة، وتاريخ الترقية،
والإدارة العامة، ومدينة العمل، ومدّة الخدمة في الوظيفة الحالية. والوظائف
السابقة كسبت `section` و`years`.

**«ثانوية عامة» انضمّت لقائمة المؤهلات** — وهي مكرّرة في ثمانية مواضع (ثلاثة في
الخادم وخمسة في الواجهة)، أُضيفت في كلّها.

### السيرة صارت إلزامية

لا يدخل مشاركٌ بلا سيرة، لا من النموذج اليدوي ولا من الاستيراد. الحدّ:
الحقول الثابتة (الميلاد والتعيين والرتبة والإدارة والمنطقة) — وكانت إلزاميةً
**داخل** الوثيقة أصلاً — مع مؤهلٍ علمي واحد على الأقل.

كانت اختيارية، فكان يدخل مشاركٌ بلا سيرة ثم يقف عند الترشيح بلا سببٍ ظاهر.

### الاستيراد يقرأ النموذج كاملاً

الالتقاط بأسماء الأعمدة لا بترتيبها (وهو معمارٌ كان قائماً)، ثم **طيُّ** الأعمدة
المسطّحة إلى وثيقة السيرة: أربع وظائف سابقة، وأربعة مؤهلات، وستّ وعشرون دورة.
والتحقّق بـ`CvValidator` نفسه الذي يتحقّق من النموذج اليدوي — مسارٌ واحد، فلا
يدخل من الباب الجماعي ما يردّه الفردي.

وثلاث مواءمات لِما يحمله النموذج فعلاً:

- النموذج يصف الوظيفة السابقة بـ**مدّة نصّية** («٣ سنوات») لا بسنتَي بداية
  ونهاية. تُحفظ كما وردت، وتبقى السنتان لمن يُدخل يدوياً فيعرفهما.
- **سنة التخرّج وسنة الدورة** لا عمود لهما — اشتراطُهما كان يردّ كل ملفّ وارد.
- **سقف الدورات رُفع من ٢٠ إلى ٣٠**: النموذج فيه ٢٦ خانة، ومن ملأها كلّها كان
  يُردّ بخطأ ٤١٣ قبل أن يصل التحقّق أصلاً.

والتواريخ تُقرأ بثلاث صيغ — نصّاً، ورقماً تسلسلياً من Excel، ويوم/شهر/سنة —
وبلا ذلك يُردّ صفٌّ بتاريخٍ «غير صالح» وهو صحيح في الملفّ الذي يراه صاحبه.

والقالب المُنزَّل يُبنى من **نفس تعريفات الأعمدة** التي يقرأ بها المستورِد، فلا
يفترق ما نُصدره عمّا نقبله. ومعه ورقتان: المجالات الفنية وكيف تُعلَّم، والسيرة
وما هو إلزامي فيها.

### ما لم يُمَسّ

`schedules` و`assessments` وسير عمل الموجة والتقارير — بلا تغيير. والمشاركون
القائمون بلا سيرة يبقون كما هم: القاعدة الجديدة تسري على الداخل الجديد، ولا
هجرةَ تعبئةٍ رجعية.

### التحقّق

`916` اختباراً تمرّ (زادت اثنا عشر)، منها `ParticipantImportContractTest`:
الرفض بلا سيرة، وبلا مؤهل، وبلا مجال، والمؤهل المجهول يذكر المقبول، وصفٌّ
مرفوض لا يترك شيئاً خلفه، والأسباب تُقال **مجتمعة** لا واحداً بعد واحد.

> الحزمة تُشغَّل على قاعدة اختبارٍ خاصّة عند فتح أكثر من جلسة على المشروع:
> `DB_DATABASE=kafaat_test_solo php artisan test` — وإلّا تعارضت الأقفال
> وسقطت اختباراتٌ سليمة بأرقامٍ تختلف كل مرّة.

### ما يلزم لتشغيله

```bash
./backup-db.sh
php artisan migrate     # جدول المجالات + عمود الجنس
```

### كيف يُرجَع عنه

```bash
git revert <sha>..HEAD
php artisan migrate:rollback --step=2
```

## ٢٠٢٦-٠٨-٢٢ — الاسم الواحد: «المشارك» في كل نصّ يقرؤه إنسان

**الوسم:** `checkpoint/participant-naming` *(يُوسَم بعد التحقّق)*

المنصّة كانت تسمّي الشخص باسمين: «المشارك» في رمزه وبطاقته وتصريحه، والاسم
القديم في كل رسالة خطأ وعنوان شاشة. الاسمان لشيء واحد، وازدواجُهما يجعل
المستخدم يسأل: أهما كيانان؟ الآن اسمٌ واحد: **المشارك**.

### ما أُضيف

استبدالٌ نصّي في **١٥٧٢ موضعاً** عبر الشيفرة والاختبارات والوثائق وملفّات
النشر. الجذر وحده يُستبدل، فتبقى السوابق واللواحق والتشكيل سليمة —
والجمع يصير جمعاً، والمنصوب منصوباً، وحرف الجرّ في موضعه.

### ما لم يُمَسّ — وهو الأهم

**لا شيء في القاعدة ولا في الواجهة البرمجية.** جدول `candidates` باسمه،
وأعمدته بأسمائها، ومسارات `/api/candidates`، ومفاتيح الصلاحيات `candidate.*`
كما هي. التغيير في طبقة العرض وحدها — فلا هجرة، ولا كسرَ لأي عميل قائم،
ولا حاجةَ لإعادة بذر الصلاحيات.

**وثلاثة مواضع استُثنيت بقصد:**

- `ExecutiveAnalyticsService` و`USER_GUIDE.md`: الكلمة فيهما تصف **كفاءةً**
  مطروحةً لبرنامج تطوير لا شخصاً — والاستبدال هناك يُنتج جملةً بلا معنى.
- `frontend/erp-addons/`: نظامٌ آخر. الكلمة فيه متقدّمٌ لوظيفة في التوظيف،
  ومورّدٌ في منافسة — معنيان لا علاقة لهما بمشارِك التقييم.

وصفوف القاعدة القائمة تبقى بنصّها القديم (مثل `drop_reason` في مقترحات
التوزيع): النصّ مكتوبٌ لحظة حدوثه، وإعادةُ كتابة التاريخ تُفسده.

> **لمن يعيد تشغيل سكربت الاستبدال:** هذا المدخل نفسه كان يُتلَف به، لأنه
> يقتبس الكلمة القديمة ليشرحها. لذلك لا تُذكر هنا صراحةً — الوصفُ يكفي.

### التحقّق

`904` اختباراً تمرّ، و`132` ملف PHP سليمة نحوياً، وبناء الواجهة نظيف.

### كيف يُرجَع عنه

```bash
git revert <sha>      # التغيير نصّي بحت — لا هجرة تُعكَس ولا بيانات تُستعاد
```

---

## ٢٠٢٦-٠٨-٢١ — تصاريح الدخول، وملفّ كل قطاع، وتسليم الجهات

**الوسم:** `checkpoint/dispatch-and-documents` *(يُوسَم بعد التحقّق)*

بهذه الخطوات الثلاث اكتمل مخطّط «إجراءات الجدولة» كلّه.

### الخطوة ٩ — تصاريح الدخول

`EntryPermitService` على نمط `ParticipantCardService`، و`GET /schedules/permits`.

**تصريحٌ واحد لكل مشارك لا لكل جلسة**: يُقدَّم عند البوّابة مرّةً واحدة، فسردُ
ثلاثة أوقاتٍ عليه يُربك الحارس. والموعد المطبوع أبكرُ جلساته في ذلك اليوم.

**والاسم بشرطين معاً**: طلبٌ صريح (`showName=1`) وصلاحية `candidate.view_names` —
وهو عرف كشف الحضور نفسه مع رقم الهوية لا استثناءً اخترعته. السبب أن الحارس
يطابق التصريح بشخصٍ أمامه، فالقرار للمركز والنظام يقيّده بالصلاحية ويدوّن
استعماله.

### الخطوة ١٢ — ملفٌّ لكل قطاع

`/roster/document` صار يقبل `?sectorId`، و`GET /roster/sectors` يُرجع قطاعات
اليوم وأعدادها. كان القطاع يُشتقّ من المستخدم وحده، فمدير المركز غير المحصور
**لا يستطيع طلب قطاعٍ بعينه** — وهي الخطوة نفسها. والمحصور يبقى مشدوداً لقطاعه
مهما طلب: تضييقٌ لا توسيع، ويثبته اختبار.

ونافذة **«مستندات اليوم»** في شاشة الجدولة تجمعها: التصاريح والبطاقات وكشف
اليوم، ثم زرٌّ يفتح ملفّاً لكل قطاع في تبويب.

### الخطوة ١١ — تسليم الجهات

`dispatch_authorities` + `schedule_dispatches`، وصلاحية `schedule.dispatch`
لمدير المركز وحده.

**التقسيم على فئة المشارك لا على قطاعه.** الجلسة الأخرى نقلت التمييز
العسكري/المدني من `sectors.is_military` إلى `candidates.personnel_category`
وهي مُحقّة: القطاع جهةٌ يعمل فيها الجميع معاً، وربطُ الجهة به كان يُرسل مدنيّ
«الأمن العام» إلى وكالة الشؤون العسكرية. بُني التسليم على الفئة.

**والفئات صارت ثلاثاً والمخطّط يذكر اثنتين** — فبدل التخمين في الشيفرة صار
الربط **بياناً**: لكل جهة عمود `categories`. البذرة تضع المتعاقد مع الموارد
البشرية، وهو افتراضٌ ظاهر يُغيَّر بتعديل صفّ — واختبارٌ يثبت أن نقله لجهةٍ
أخرى يسري بلا لمس شيفرة.

**بصمة SHA-256 على ما خرج فعلاً**، ومحضرُ تسليمٍ يحمل البصمة والتوقيعين. وبلا
اتصال خارجي: المخرج ملفٌّ يُنزَّل ومحضرٌ يُطبع — إخراج البيانات إلى قناةٍ
خارجية قرارٌ يُتخذ صراحةً لا يُستنتج من ميزة.

### الخطوة ١١ صارت آلية — والتاسعة والثانية عشرة تبقيان يدويتين عمداً

`period.dispatched` يكتمل حين تُسلَّم لكل جهةٍ **لها مشاركون** في الموجة —
والجهة بلا مشاركين لا تُنتظر، فدورةٌ بلا عسكريين لا تقف على وكالة الشؤون
العسكرية.

أمّا **الطباعة وحفظ الملفّ فعلان خارج المنصّة لا تراهما**: خروجُ الورق من
الطابعة ليس حدثاً في قاعدة البيانات، وإعلانُ اكتماله من سجلّ قراءةٍ ادّعاءٌ لا
قياس. تبقيان خانتَي تأشير — وهذا أصدق من فحصٍ يكذب.

**١٥ اختباراً** في `DispatchAndDocumentsTest`. الحزمة: **٨٨٢ ناجحة**.

### ما يلزم لتشغيله

```bash
./backup-db.sh
php artisan migrate     # الجهتان مبذورتان + سجلّ التسليم + منح الصلاحية + ربط الفحص
```

### كيف يُرجَع عنه

```bash
git revert <sha>..HEAD
php artisan migrate:rollback --step=3
```

---

## ٢٠٢٦-٠٨-٢٠ — الجدول الذهبي، وتاريخ التقييم حقلاً لا انضماماً

**الوسم:** `checkpoint/golden-schedule` *(يُوسَم بعد التحقّق)*

### الخطوة السادسة: الجدول الذهبي **سجلٌّ يُكتب** لا عرضٌ محسوب

لو كان إسقاطاً من `schedules` لتغيّر أثراً رجعياً كلما حُذفت جلسة أو نُقلت.
والمخطّط يصف سجلّاً: ما أُثبت لهذه الدورة يبقى كما أُثبت. لذلك
`golden_schedule_entries` **تنسخ** التاريخ والرمز ولا تجلبهما بانضمام.

- **المزامنة idempotent**: تُشغَّل مرّتين بلا مضاعفة (قيد فريد على
  الموجة+التاريخ+الرمز).
- **`source` يفصل المُرحَّل عن المكتوب بيد**: إعادة المزامنة تُحدّث الأول
  و**لا تمسّ الثاني** — صفٌّ أُضيف يدوياً لسببٍ إداري لا يمحوه زرٌّ بعد أسبوع.
  ويظهر في المستند بإطارٍ مذهَّب.
- **الحصر يسري على الرموز**: رمزُ مشاركٍ مصنّف لا يظهر لمن لا يملك تصريحه —
  ولا في المستند المطبوع. والمحصور بقطاع يرى قطاعه وحده مهما طلب.
- **مستند A4 أفقي** بعرف الطباعة نفسه (heredoc واحد، `@page`، شريط طباعة يُخفى
  عند الطبع، الشعار قاعدةَ CSS واحدة).

### الخطوة العاشرة: التاريخ صار حقلاً

`assessments.first_session_date` و`last_session_date` — كان التاريخ علاقةً
تُجلب بانضمام، فلا يُفلتَر ولا يُصدَّر ولا يُقرأ في بطاقة المشارك. عمودان
محسوبان، **بتعبئة رجعية على دفعات** في الهجرة نفسها.

و`Assessment::refreshSessionDates()` تُستدعى من **ستة كتّاب**: إنشاء الجلسة
وتعديلها وحذفها وإعادة جدولتها، واعتماد التوزيع الأسبوعي، واعتماد الاستقبال —
ومعها إسناد حلقة النقاش وسحبه ونقل الحلقة. دالّةٌ واحدة وتسعة مواضع نداء، لا
تسعة حسابات متفرّقة تنحرف.

### الخطوتان صارتا آليّتين في سير العمل

هذا ما وعد به تصميم سير العمل: الخطوة اليدوية تصير آليةً **بكتابة مفتاحها** حين
تُبنى قدرتها. أُضيف فحصان — `period.golden_synced` و`period.dates_written` —
وهجرةٌ تربطهما بالخطوتين ٦ و١٠ في المنصّات المبذورة سلفاً (مشروطةً بأن يكون
المفتاح فارغاً، فمن غيّر الخطوة بيده لا يُنقض قراره).

و`period.golden_synced` يقارن **الأزواج لا الأعداد**: جدولٌ فيه صفوفٌ يدوية بعدد
الناقص كان سيُقرأ «مكتملاً» وهو يغفل جلساتٍ حقيقية. وجلسةٌ تُضاف بعد المزامنة
تُعيد الخطوة معلّقةً من نفسها.

**١٥ اختباراً** في `GoldenScheduleTest`.

### ما لم يُمَسّ

`schedules` بلا تغيير في سلوكها. والتوزيع الأسبوعي والاستقبال يعملان كما هما —
أُضيف لكلٍّ منهما سطرُ تحديثٍ للتاريخ لا أكثر. ولا تغيير في كشف اليوم ولا
البطاقات ولا التقارير.

### ما يلزم لتشغيله

```bash
./backup-db.sh
php artisan migrate     # الجدول + عمودان بتعبئة رجعية + ربط الفحصين
```

### كيف يُرجَع عنه

```bash
git revert <sha>..HEAD
php artisan migrate:rollback --step=2
```

---

## ٢٠٢٦-٠٨-١٩ — حلقة النقاش ككيان: سعةٌ ومستشارٌ ومجموعة

**الوسم:** `checkpoint/discussion-circles` *(يُوسَم بعد التحقّق)*

### لماذا

المقابلة جلسةٌ لمشاركٍ واحد، أمّا حلقة النقاش فجلسةٌ **لمجموعة**. تمثيلها بصفوف
`schedules` منفصلة كان يعمل ولا يعبّر عن السعة: لا شيء يمنع إسناد اثني عشر
مشاركاً إلى حلقةٍ تتّسع لستّة، ولا شيء يمنع حجز المستشار نفسه في حلقتين
متزامنتين.

### قراران تُرِكا إعداداً لا تخميناً

**السعة**: عمودٌ لكل حلقة، وقيمته الافتراضية من الإعداد
`discussion.default_circle_capacity` (٦). المركز يضبط رقمه مرّة، والحلقة
الاستثنائية تتجاوزه — ولا رقمَ محفورٌ في الشيفرة.

**العلاقة بمجموعتَي الكشف (أ/ب)**: `group_letter` **قابل للإفراغ**. إن كانت
الحلقة تقابل المجموعة واحدةً بواحدة يُملأ الحرف، وإن كانتا مستقلّتين يُترك
فارغاً. أيّ الاحتمالين صحّ لا يحتاج هجرةً ثانية.

### ما أُضيف

- **`discussion_circles`** + عمود `schedules.circle_id` قابل للإفراغ.
- **قيد فريد جزئي** `(evaluator_id, circle_date, circle_time) WHERE evaluator_id IS NOT NULL`
  — المستشار لا يقع في حلقتين في اللحظة نفسها، والقيد هو الحارس لا فحصٌ تسبقه
  ضغطتان متزامنتان.
- **الإسناد يُنشئ صفوف `schedules` عادية** (نشاطها `discussion`، وموعدها
  ومستشارها من الحلقة)، فالحضور وكشف اليوم والتقييم والتقارير تلتقطها **بلا
  تعديل حرفٍ فيها**. الحلقة طبقةٌ فوق الجلسات لا بديلٌ عنها.
- **السعة تُفرض داخل معاملة بقفل صفّي**: العدّ بعد القفل لا قبله، وما تجاوزها
  يُردّ في `skipped` بسببه بدل أن يسقط صامتاً.
- **الحلقة تجرّ جلساتها**: نقل موعدها أو تغيير مستشارها يُحدّث صفوفها كلها —
  بلا ذلك يبقى المشارك على موعدٍ قديم بينما الحلقة انتقلت.
- **شاشة «حلقات النقاش»** في القائمة الجانبية تحت الجدولة.

**١٨ اختباراً** في `DiscussionCircleTest`.

### علّتان اكتُشفتا أثناء البناء

**١) `circle_id` لم يكن في `$fillable`** — فالإسناد الجماعي يُسقطه بصمت وتُنشأ
الجلسة بلا حلقة: العملية «تنجح» والحلقة تبقى فارغة. التقطها اختبار.

**٢) انتهاك القيد كان يُجهض المعاملة المحيطة كلها.** في postgres يُبطِل خطأُ
عبارةٍ المعاملةَ حتى نهايتها (`25P02`)، فالتقاطُ `UniqueConstraintViolationException`
وردُّ ٤٠٩ يترك الاتصال معطّلاً لكل ما بعده. صار الإدراج داخل معاملته الخاصة
فيعود الفشل إلى نقطة حفظ. **العلّة نفسها كانت كامنة في متحكّم الموجات**
(الاسم الفريد) وأُصلحت معه.

### ما لم يُمَسّ

الجلسة بلا حلقة تُنشأ وتعمل كما كانت. ولا تغيير في الحضور ولا كشف اليوم ولا
مجموعتَي أ/ب — الحرف رابطٌ اختياري لا استبدال.

### ما يلزم لتشغيله

```bash
./backup-db.sh
php artisan migrate     # الجدول + العمود + الإعداد الافتراضي
```

### كيف يُرجَع عنه

```bash
git revert <sha>..HEAD
php artisan migrate:rollback --step=1
```

---

## ٢٠٢٦-٠٨-١٨ — الإسناد لكل نشاط، والمساعد، والخبرات

**الوسم:** `checkpoint/expertise-and-seats` *(يُوسَم بعد التحقّق)*

### ثلاث علل قائمة أُصلحت

**١) حلقة النقاش كانت تُحفظ بلا مقيّم.** `Schedules.vue` كانت تكتب
`activity === 'interview' ? evaluatorId : null` — فأي نشاطٍ غير المقابلة يفقد
مقيّمه مهما اختار المستخدم، ثم لا يجد المستشار جلسته في شاشته. صار المقيّم
يُرسَل لكل نشاط.

**٢) المساعد لم يكن قابلاً للإسناد.** العمود `schedules.assistant_id` موجود منذ
البداية ويُطبع في كشف الحضور، ولم يكن في المنصّة **حقلٌ في الشاشة** ولا **مسارٌ
يُرجع قائمة المؤهّلين**. صار له مقعده في `/candidates/{id}/assessors?seat=assistant`
وحقله في النموذج، ويعود `assistantId` في `/schedules` (كان يُكتب ولا يُقرأ).

**٣) مصافحة تجاوز القطاع كانت مبنيّة في الخادم ومهملة في الواجهة.** الردّ ٤٠٩ مع
`requiresConfirmation` كان يُعرض كخطأٍ غامض بلا إعادة إرسال — فبدا التجاوز
مستحيلاً وهو مسموحٌ بتأكيد. صارت الواجهة تسأل ثم تُعيد الإرسال بـ`confirmCrossSector`.

وتسمية المقعد صارت تتبع النشاط: «مستشار حلقة النقاش» لا «مستشار المقابلة».

### ما أُضيف: مجالات الخبرة (الخطوة السابعة)

`expertise_areas` + `user_expertise` — مرجعٌ يُدار من الإعدادات كالرتب، تُوسَم به
حسابات المقيّمين من شاشة المستخدمين. وعند اختيار المستشار تُقرأ سيرة المشارك
(المنصب والنبذة والخبرات والشهادات والمؤهلات) وتُقارَن بوسم كل مقيّم، فيعود مع
كل اسم `matchedAreas` و`matchScore`، ويتقدّم الأقرب.

**اقتراح ترتيبٍ لا حجب** — كما هو حال النصاب: من درجته صفر يبقى في القائمة
قابلاً للاختيار، والقرار للمُجدوِل. والمطابقة على نصٍّ مطبَّع (تُزال التشكيلات
وتُوحَّد الهمزات والتاء المربوطة والياء) نظير ما يفعله `ImportController`.

**١٧ اختباراً** (`DiscussionAssignmentTest` ٨ و`ExpertiseMatchTest` ٩).

### علّة أمنية التقطها اختبارٌ ثابت قائم

`RouteAuthorizationSweepTest` كشف أن `GET /expertise-areas` يبلغه **أدنى دور في
النظام**. كنت فتحته «مرجعاً للجميع» قياساً على `/ranks` و`/sectors` — والقياس
خطأ: تلك يفتحها نموذج الترشيح للجهة الخارجية، أمّا مجالات الخبرة فلا يقرؤها إلا
من يحرّرها (`settings.manage`) أو يوسم بها حساباً (`user.manage`). وشاشة الجدولة
لا تطلبها أصلاً — المطابقة تُحسب في الخادم. أُغلق المسار بدل توسيع قائمة
الاستثناءات في الاختبار.

### ما لم يُمَسّ

`GET /candidates/{id}/interviewers` بمساره وشكل استجابته. والجلسة بلا موجة وبلا
مساعد تُنشأ كما كانت. ولا تغيير في التوزيع الأسبوعي ولا الاستقبال ولا الحضور.

### ما يلزم لتشغيله

```bash
./backup-db.sh
php artisan migrate     # هجرة واحدة: expertise_areas + user_expertise
```

ثم تُضاف المجالات من **الإعدادات ← مجالات الخبرة**، وتُوسَم بها حسابات المقيّمين
من **المستخدمون ← أيقونة المصباح**. وبلا مجالات تعمل الشاشات كما كانت: الدرجة
صفر للجميع والترتيب بالحمل.

### كيف يُرجَع عنه

```bash
git revert <sha>..HEAD
php artisan migrate:rollback --step=1
```

---

## ٢٠٢٦-٠٨-١٧ — سير عمل الجدولة: اثنتا عشرة خطوة بياناتٍ لا شيفرة

**الوسم:** `checkpoint/scheduling-workflow` *(يُوسَم بعد التحقّق بمتصفّح حقيقي)*

### لماذا

مخطّط «إجراءات الجدولة» ورقةٌ إدارية باثنتي عشرة خطوة. تثبيتها في الشيفرة
يجعل كل تعديلٍ إداري إصداراً: تُدمج خطوتان فيلزم نشر، أو يُضيف تعميمٌ خطوةً
فتنتظر مبرمجاً. فصارت **صفوفاً في جدول** تُحرَّر من شاشة الإعدادات.

### ما أُضيف

**١) `scheduling_workflow_steps`** — الخطوات الاثنتا عشرة **مبذورةً في الهجرة
نفسها** بنصّ المخطّط وترتيبه. لكل خطوة عنوان ووصف وترتيب، و`is_active`
و`is_required`، و`auto_key`.

**٢) `auto_key` هو الفكرة كلّها.** مفتاحٌ من قائمة معروفة في
`SchedulingWorkflowService::CHECKS` يُحسب حيّاً من حالة الموجة، وفارغٌ يعني
خانةَ تأشيرٍ يدوية. خمسٌ من الاثنتي عشرة آليةٌ اليوم (المدى، اللوحة، إسناد
النشاط، وجود الجلسات، الاعتماد، وربط المقيّمين)، والباقي يدويٌّ لأن قدرته لم
تُبنَ بعد — **وتصير آليةً بكتابة مفتاحها** حين تُبنى، بلا هجرة ولا نشر.

**٣) الآلية تُقاس ولا تُخزَّن.** تأشيرها يدوياً مرفوض (٤٢٢)، وتعود معلّقةً من
نفسها إن نُقض شرطها — سحبُ آخر اسم من اللوحة يُعيد خطوة «اختيار المقيمين»
معلّقة. تخزينها كان سيعني «مكتملة» بجانب شرطٍ منقوض.

**٤) `period_step_progress`** — تأشير الخطوات اليدوية لكل موجة، بثلاث حالات:
معلّقة، تمّت، و**«لا تنطبق» بسببٍ إلزامي** تُخرج الخطوة من **مقام** النسبة لا
من بسطها: دورةٌ بلا عسكريين لا تنتظر خطوة وكالة الشؤون العسكرية إلى الأبد.

**٥) بطاقة «سير عمل الجدولة» في الإعدادات** — تحرير النصّ والوصف، ونقلٌ
بالسهمين، وربطٌ بفحصٍ آلي من قائمة مغلقة، وإلزامية/تفعيل، وإضافة وحذف.
وقائمةُ الخطوات في شاشة الموجة بشريط إنجاز.

**١٩ اختباراً** في `SchedulingWorkflowTest`.

### الضوابط المقصودة

- **مفتاح فحصٍ غير معروف يُرفض عند الحفظ** — خطوةٌ «آلية» لا أحد يحسبها تبقى
  معلّقةً إلى الأبد. ومفتاحٌ بقي في القاعدة بعد حذفه من الشيفرة يُقرأ **غير
  مكتمل** لا مكتملاً: التساهل هنا يُعلن إنجازاً لم يقع.
- **لا يُحذف آخر خطوة.** والإطفاء بديلٌ عن الحذف لمن يريد إبقاء السجلّ.
- **إعادة الترتيب تُرسَل كاملة** — قائمةٌ ناقصة تترك خطوةً بترتيبٍ قديم فتقفز
  إلى موضعٍ لم يخترْه أحد.
- التعريف في `PlatformReset::KEEP` لا `REFERENCE`: بذرُه في الهجرة لا في بذرةٍ
  تُعاد، فمسحُه يترك المنصّة بلا إجراءٍ ولا سبيل لاستعادته إلا بإعادة الهجرة.

### ما لم يُمَسّ

`workflow_stages` (سلسلة اعتماد التقرير) وشاشتها كما هي — سير عمل الجدولة
جدولٌ مستقل. والموجات وشاشة الجدولة والتوزيع الأسبوعي بلا تغيير في سلوكها:
القائمة إضافةٌ تُعرض، لا حارسٌ يمنع. **لا خطوة تمنع إرسال الموجة أو اعتمادها**
— القائمة تُقرأ ولا تُلزِم.

### ما يلزم لتشغيله

```bash
./backup-db.sh
php artisan migrate     # هجرتان: جدول الخطوات (مبذوراً) وجدول التأشير
```

### كيف يُرجَع عنه

```bash
git revert <sha>..HEAD
php artisan migrate:rollback --step=2
```

---

## ٢٠٢٦-٠٨-١٦ — موجة الجدولة: تواريخها، ولوحة أسمائها ونصابها، واعتماد مدير المركز

**الوسم:** `checkpoint/scheduling-periods` *(يُوسَم بعد التحقّق بمتصفّح حقيقي)*

### لماذا

مخطّط «إجراءات الجدولة» يبدأ بأربع خطوات لم يكن لواحدةٍ منها موضعٌ في المنصّة:
تحديد تواريخ الدورة، واختيار المقيّمين والمساعدين، وتحديد نصاب كل مقيّم، ثم
إرسال الجدولة لمدير المركز. التواريخ كانت **ثابتةً في الشيفرة** —
`now()->next(SUNDAY)` وخمسة أيام في ثابتٍ خاص — فلا يمكن التعبير عن دورةٍ من
ثلاثة أيام. والنصاب كان قيمةً واحدة للجميع. والمساعد كان عموداً في قاعدة
البيانات يُطبع في كشف الحضور **بلا أي مسارٍ يُرجع قائمته** ولا حقلٍ في الشاشة.
ومدير المركز ومسؤول الجدولة كانا يحملان الصلاحية نفسها، فمن يبني الجدول هو من
يعتمده وخطوة «الإرسال للاعتماد» بلا معنى تقني.

والجدولة **يدوية بقرار صاحب المنصّة**: التوزيع الآلي يبقى كما هو خياراً مساعداً،
والمستخدم يختار الأسماء والمشاركين بنفسه.

### ما أُضيف

**١) جدولان جديدان.** `scheduling_periods` (اسم فريد، مدى، أوقات جلسات
اختيارية، حالة، وأعمدة الإرسال والاعتماد والرفض) و`period_assessors`
(الموجة × الشخص × النشاط × المقعد، ومعها `daily_quota` و`period_quota`
و`is_available`). و**عمود واحد** على `schedules`: `period_id` قابل للإفراغ.

**٢) النصاب صار لكل اسم.** فارغاً يقع على الإعداد العام
`distribution.daily_cap_per_evaluator` كما كان — فمركزٌ لم يضبط نصاباً لأحد يعمل
كما كان يعمل. و**هو عدّاد لا سدّ**: الشاشة تعرض «٣/٥» وتُحمّر المتجاوز ولا
تمنعه، لأن القرار صار للمستخدم لا للخوارزمية.

**٣) المساعد صار قابلاً للإسناد.** `GET /candidates/{id}/assessors` تعميمٌ
لـ`interviewers` يقبل `activity` و`seat`، ويحلّ أدوار الأهلية من
`ReceptionAssignment::ACTIVITY_ROLES` بدل `where('code','EVALUATOR')` الحرفية.
ومع `periodId` يعود كل اسمٍ ومعه نصابه وحمله، مرتّبين بالمُدرَج ثم الأخفّ حملاً.

**٤) صلاحية `schedule.approve_center`** — لمدير المركز وحده، وليست في
`NON_DELEGABLE` فتُفوَّض لشخصٍ بعينه حين يغيب. مسار الموجة:
مسودّة ← بانتظار الاعتماد ← معتمَدة ← مغلقة، والرفض يعيدها مسودّةً **بسببٍ
مكتوب** يصل لمن أرسلها. الإشعار بـ`notifyPermission` لا `notifyRole` — مركزٌ لم
يُنشئ دور مدير المركز كان الإشعار فيه يذهب إلى لا أحد فتقف الموجة صامتة.

**٥) `GET /scheduling-periods/{id}/eligible`** — أسماء من يصلح للإدراج، بدل فتح
`/users` لمسؤول الجدولة: قائمة المستخدمين سلطةُ نظام لا حاجةَ جدولة.

**٦) شاشة `SchedulingPeriods.vue`** ورابطها في القائمة الجانبية فوق «الجدولة».

**٢٦ اختباراً** في `SchedulingPeriodTest`.

### ما لم يُمَسّ

- **`GET /candidates/{id}/interviewers` بمساره وشكل استجابته** — صار غلافاً
  لـ`assessors()` ويردّ ما كان يردّه؛ و`InterviewerAssignmentTest` يمرّ بلا تعديل.
- **الجلسة بلا موجة** تُنشأ وتُعدَّل كما كانت تماماً. `period_id` قابل للإفراغ
  وكل صفٍّ قائم بقي `null`.
- **التوزيع الأسبوعي** (`DistributionService`/`DistributionController`) بلا سطرٍ
  واحد مُعدَّل — يبقى المسار الآلي عاملاً بجانب اليدوي.
- الحضور والتقييم والتقارير وكشف اليوم كما هي.

### ما يلزم لتشغيله

```bash
./backup-db.sh                # في جذر المستودع الأعلى، قبل الهجرة
php artisan migrate           # أربع هجرات: جدولان، عمود، ومنح الصلاحية
```

الهجرة الرابعة تمنح `schedule.approve_center` لدور `CENTER_MANAGER` في
`role_permissions` — وتتخطّى دوراً جُرّد عمداً من الشاشة، ودوراً بلا صفوف (يقع
على المصفوفة وقد أُضيفت فيها).

**بدون مدير مركز فعّال** يبقى المسار عاملاً حتى «بانتظار الاعتماد» ثم يقف؛
والاستجابة تقول صراحةً «لا يوجد حاملٌ لصلاحية الاعتماد لإشعاره» بدل صمتٍ يُوهم
الإرسال.

### كيف يُرجَع عنه

```bash
git revert <sha>..HEAD                    # تراجع يُبقي التاريخ
php artisan migrate:rollback --step=4     # يُسقط الجدولين والعمود والمنح
```

الرابط وحده يُخفى بـ`FEATURES` في الواجهة دون مسّ البيانات.

---

## ٢٠٢٦-٠٨-٠٨ — الترقيم يعمّ القوائم الأربع، ومنطقه في مكانٍ واحد

**الوسم:** `checkpoint/list-paging`

### ما أُضيف

**١) الترقيم والفرز صارا في المتحكّم الأساس** (`applyListPaging`) بدل أن
يُنسَخا. أربع قوائم تحتاجه ولكلٍّ فخّها نفسه — فرزٌ بلا فاصلٍ ثابت، وعمود
فرزٍ يأتي من المستخدم، وعميلٌ ينكسر لو صار الترقيم افتراضاً. تُحلّ مرّة.

**٢) `/reports` و`/evaluations` و`/users` تقبل `page`/`perPage`/`sort`/`dir`**
بالسلوك نفسه، وتعيد `meta`.

| المسار | أعمدة الفرز | الافتراضي |
|---|---|---|
| `/candidates` | `code`·`sector`·`rank`·`tier`·`status`·`classification`·`created` | `code` تصاعدياً |
| `/reports` | `created`·`code`·`status`·`recommendation`·`behavioral`·`technical`·`returns` | `created` **تنازلياً** |
| `/evaluations` | `updated`·`code`·`status`·`activity` | `updated` **تنازلياً** |
| `/users` | `name`·`username`·`role`·`active`·`lastLogin`·`created` | `name` تصاعدياً |

**٣) شاشة التقارير تُرقَّم وتُفرَز** — شريط فرز بسبعة أعمدة وزرّ اتجاه،
وشريط ترقيم، وحجم صفحة يختاره المستخدم.

### علّتان أُصلحتا في الطريق

- **حصر التصنيف في التقييمات كان يجري بعد الجلب** (`->get()->filter(...)`).
  مع الترقيم يصير ذلك خطأً لا بطئاً: تُجلب خمسون فيُقصّ منها ثلاثون فتُعرَض
  عشرون على أنّها صفحةٌ كاملة، **والعدد الكلي يحسب المحجوبين**. انتقل إلى SQL.
- **الاتجاه الافتراضي.** `/reports` و`/evaluations` كانتا تعرضان الأحدث أولاً.
  ترقيمٌ يثبّت الاتجاه على «تصاعدي» كان سيقلبهما إلى الأقدم أولاً بلا أن يطلب
  أحد. صار الاتجاه الافتراضي معامِلاً لكل قائمة.
- **والفاصل الثابت يتبع اتجاه الفرز** لا يبقى تصاعدياً: قائمةٌ بالأحدث أولاً
  فيها صفّان في الثانية نفسها يجب أن يتقدّم أحدثُهما.

### ما لم يُرقَّم عمداً

**شاشة المستخدمين**: أربعة عشر صفّاً، و`managerOptions` تُبنى من القائمة
المحمَّلة — فترقيمها يجعل قائمة اختيار المدير تعرض من في الصفحة الحالية وحدهم.
**شاشة التقييم**: تعرض مسودات المقيّم نفسه، قائمة عملٍ لا تصفّح.

المساران يقبلان الترقيم للمتكامِل، والشاشتان لا تطلبانه.

### ما لم يُمَسّ

الترقيم بطلبٍ صريح في الأربعة: بلا `page` ولا `perPage` تعود القائمة كاملةً
كما كانت. ولا تغيير في `/reports/analytics` — تُحسب على الترشيح كلّه لا على
الصفحة، وإلا صار الرسم البياني يصف خمسةً وعشرين تقريراً ويُقرأ كأنّه يصف الكل.

### ما يلزم لتشغيله

لا شيء. لا هجرة ولا إعداد.

### كيف يُرجَع عنه

```bash
git revert <sha>     # في المستودعين
```

**١٢ اختباراً جديداً** في `ListPagingTest`. الحزمة: ٧٤٦ ناجحة.

---

## ٢٠٢٦-٠٨-٠٧ — ترقيم قائمة المشاركين وفرزها على الخادم

**الوسم:** `checkpoint/candidate-paging`

### ما أُضيف

**١) `GET /candidates` يُرقَّم ويُفرَز على الخادم.** أربعة معاملات جديدة:

| المُعامِل | القيم | الافتراضي |
|---|---|---|
| `page` | ≥ ١ — صفحةٌ تجاوزت الآخر **تُشدّ إلى الأخيرة** لا تعود فارغة | — |
| `perPage` | ١–٢٠٠ | ٥٠ عند طلب صفحة |
| `sort` | `code`·`sector`·`rank`·`tier`·`status`·`classification`·`created` | `code` |
| `dir` | `asc`·`desc` | `asc` |

والاستجابة تحمل `meta` **إضافةً** لا بديلاً: `total` و`shown` و`page`
و`perPage` و`lastPage` و`sort` و`dir` و`truncated`.

**٢) الشاشة تطلب صفحةً لا الجدول كلّه** — شريط ترقيم، وترويسات قابلة للفرز
بالنقر (والنقرة الثانية تعكس الاتجاه)، وحجم صفحة يختاره المستخدم.

**٣) البحث صار مُهدَّأً (٣٠٠ مللي).** كان `@input="load"` يستدعي الخادم عند
**كل حرف**: رمزٌ من ثمانية أحرف = ثمانية استعلامات على الجدول كلّه، سبعةٌ
منها تُرمى قبل أن تُعرَض. قيس في المتصفّح بعد التعديل: **ثمانية أحرف ⇒ طلبٌ
واحد**.

### ثلاث تفصيلات تُسقِط الترقيم لو أُغفلت

- **الفاصل الثابت.** الفرز على عمودٍ متكرّر القيم (القطاع، الحالة، الفئة)
  ترتيبُ متساوييه **غير محدَّد** في postgres، فيتنقّل الصفّ بين الصفحات:
  يُرى مرّتين ويغيب غيره. كل فرزٍ يُذيَّل بالرمز. تحقُّقٌ في المتصفّح على
  فرز القطاع: ٢٥ + ٢٠ = ٤٥، وصفر تكرار.
- **الفرز بقائمة مغلقة.** `orderBy($request->sort)` يضع نصّ المستخدم في جملة
  SQL. القائمة تُعرَّف مرّةً في `SORTABLE` وتُشتقّ منها قاعدة التحقّق، فلا
  يفترق المسموح عن المترجَم.
- **القطاع يُفرَز باسمه لا بمعرّفه**، باستعلامٍ مرتبط لا انضمام — الانضمام
  يُدخِل `sectors.id` فيتصادم مع `candidates.id` بصمت.

### ما لم يُمَسّ

**الترقيم بطلبٍ صريح.** بلا `page` ولا `perPage` تعود القائمة **كاملةً كما
كانت**، فلا ينكسر عميلٌ قائم بصمت — يرى خمسين صفّاً مكان ألف ويظنّها كلّ ما
في المركز. وفي هذه الحالة سقفٌ صلب (٥٠٠٠ صفّ) يُعلَن في `meta.truncated` بدل
صمتٍ يُوهم الاكتمال.

الحصر والفلاتر تُطبَّق **قبل** العدّ وقبل الصفحة، فـ`total` يعكس نطاق القارئ
لا الجدول. ولا تغيير في `/candidates/export` ولا `/cards` — التصدير يبقى
كاملاً.

### ما يلزم لتشغيله

لا شيء. لا هجرة ولا إعداد.

### كيف يُرجَع عنه

```bash
git revert <sha>     # في المستودعين
```

**١٢ اختباراً جديداً** في `CandidateListPagingTest` — منها التوافق مع ما كان،
وتغطية كل صفٍّ مرّةً واحدة، ورفض عمود فرزٍ مجهول، وبقاء حدّ القطاع.
الحزمة: ٧٣٤ ناجحة.

---

## ٢٠٢٦-٠٨-٠٦ — جرد سطح الواجهة البرمجية، وحارسان يمنعان انحرافه

**الوسم:** `checkpoint/api-audit`

جردٌ منهجي لـ**١٦٦ مساراً**: بوّابة كل واحد، وحدّ معدّله، وحدود قوائمه،
وموضعه من المرجع. ما وجدتُه وما بُني ليمنع عودته.

### ما أُصلح

**١) `POST /settings/ldap/test` بلا سقف معدّل** — وأخواته الثلاث
(`sms`/`smtp`/`idverify`) محدودة بخمسٍ في الدقيقة، **والمرجع يدّعي أن له
حدّاً**. المسار يفتح اتصالاً بمضيفٍ **يختاره الطالب**؛ منفذه محصور بمنافذ
LDAP والمحاولة مُدقَّقة — الدفاعان الأوّلان قائمان، والثالث وحده كان ناقصاً.
بلا سقف يصير الاختبار ماسحَ مضيفاتٍ داخلية: يكشف الموجود من المعدوم بفارق
زمن الردّ. صار `throttle:5,1`، ويحرسه ثابتٌ يفحص **كل** مسار `settings/*/test`.

**٢) قائمة `IN` بعدد المشاركين كلّهم.** `GET /candidates` كان يُنفّذ استعلام
الغياب بـ`whereIn('candidate_id', $candidates->pluck('id'))` — أي أنّ نصّ
الاستعلام ينمو بنموّ المركز. على عشرين ألف مشارك تُرسَل عشرون ألف معرّف إلى
القاعدة في كل فتحةٍ للشاشة. صار استعلاماً فرعياً: **٢٣٩ حرفاً ثابتة** مهما
كبر العدد، والسلوك كما هو (يحرسه `RescheduleTest`).

**٣) `routes/config.php` — سبعة مسارات لم يعلم بها المرجع** (`/ranks` بكاملها
وإدارة القطاعات). تُحمَّل عبر مزوّد خدمة مستقلّ لا من `bootstrap/app.php`،
فغابت عن كل جرد. و`/ranks` تخصّ المتكامِل: `rankLabel` حقلٌ إلزامي في إنشاء
المشارك، **والرتبة تقود تصنيف الفئة القيادية** — فنصٌّ حرّ لا يطابق قائمة
المركز يُدخِل المشارك في الفئة الخطأ.

### الحارسان

**`ApiSurfaceDenialTest`** — يعدّ المسارات من جدول التوجيه نفسه لا من قائمة
مكتوبة، فيغطّي ما يُضاف غداً وحده:

- أقلّ المستخدمين صلاحيةً يُمنَع من **كل** مسارٍ إلا ما استُثني صراحةً
- **ولا يُردّ عليه بـ٤٢٢**: متحكّمٌ يتحقّق من المدخلات قبل الصلاحية يُعلِّم
  غيرَ المُصرَّح له أسماء الحقول وأنواعها وحدودها قبل أن يمنعه. الترتيب قاعدة
  أمنية لا ذوق — والمنصّة تمرّ اليوم بلا استثناء واحد
- بلا رمز: كل مسارٍ محميّ يعيد ٤٠١، فلا مسار ينسى المصادقة
- كل `settings/*/test` مخنوق

**`ApiDocumentationCoverageTest`** — كل مسارٍ مسجَّل له سطر في `API.md`،
وكل مسارٍ في `API.md` موجود في الشيفرة. انحرف المرجع مرّتين هذا الأسبوع
(ثلاثون مساراً، ثمّ ملفّ مسارات كامل)؛ لا يعود بلا انتباه ما دام الفحص آلياً.

### ما لم يُمَسّ

لا مسار تغيّر عقده، ولا استجابة تغيّر شكلها، ولا صلاحية تحرّكت. `/sectors`
و`/ranks` تبقيان مفتوحتين لكل مُصادَق — قائمتان مرجعيتان يحتاجهما من يملأ
نموذجاً، وكلتاهما تحجب أعداد المرتبطين والبادئات عن غير مدير الإعدادات.

### ما قيس ولم يُغيَّر

القوائم بلا ترقيم صفحات: `/candidates` و`/reports` و`/evaluations`
و`/users` تعيد نطاقها كاملاً. القياس: **٢٠ ألف مشارك ≈ ٧١٤ مللي ثانية
و٤٫٢ ميغابايت** للطلب. لم أُضِف ترقيماً — يكسر ترشيح الشاشة ويحتاج عملاً في
الواجهة، وهو قرارٌ لصاحب المنصّة. وُثّق للمتكامِل في `API_GUIDE.md` كي يضبط
مهلته عليه.

### كيف يُرجَع عنه

```bash
git revert <sha>
```

لا هجرة ولا تغيير بيانات.

**٦ اختبارات جديدة.** الحزمة: ٧٢٢ ناجحة.

---

## ٢٠٢٦-٠٨-٠٦ — أسماء لمدير المركز، وإغلاق أربع بوّابات كانت مفتوحة

**الوسم:** `checkpoint/center-manager-names`

### ما أُضيف

**١) مدير المركز يقرأ أسماء المشاركين** (`candidate.view_names`) — بقرار صاحب
المنصّة. من ٣٧ صلاحية إلى ٣٨. الاسم يظهر له في بطاقة المشارك والسيرة الذاتية
والتصدير وكشف الحضور والتقارير وسجل المراسلات.

> **الرصد يبقى بالرمز.** المقيّم ومساعده — وهما وحدهما من يُدخل درجة — بلا
> أسماء كما كانا. حياد التقييم قائمٌ على ذلك، ومدير المركز لا يرصد.

**٢) صلاحية كل شاشة تُغلق مسارها.** أربع شاشات كانت صلاحيتها المستقلّة تُخفي
الرابط في المتصفّح **ولا تُغلق المسار على الخادم**، لأن المتحكّم كان يقبل
صلاحيةً عامّة بديلاً — إبقاءً على ما كان يعمل قبل الفصل:

| المسار | كان يقبل | صار |
|---|---|---|
| `/api/analytics/executive` | `analytics.view` | `analytics.executive` |
| `/api/daily-report` (+`/document`) | `analytics.view` | `analytics.daily_report` |
| `/api/chat/*` | `report.view` فقط | `chat.view` **+** بوّابة الكيان |
| `/api/development-plans/*` | `report.view` | `development_plan.view` |

الأثر قبل الإصلاح: مديرٌ يسحب «التقرير اليومي» من دورٍ في شاشة الأدوار، فيختفي
الرابط ويبقى `GET /api/daily-report` يخدم يوم المركز كاملاً لمن يعرف عنوانه.
**فصلُ الصلاحية بلا فصل البوّابة ليس فصلاً** — وقد سُلّمت هذه الشاشات على أنّ
لكلٍّ صلاحيتها، فكان اليقين كاذباً.

**٣) دليل مستخدم للواجهة البرمجية** — `docs/API_GUIDE.md`: عميل جاهز
(Python/PowerShell/cURL)، وسبع وصفات كاملة (إضافة مشارك، طلب تحديث، مسار
الاستقبال بمراحله الستّ، التقييم إلى تقرير معتمد، سحب المؤشرات، ضبط الأدوار)،
وجدول أخطاء يقول لكل حالة ماذا يفعل العميل، وقائمة تحقّق قبل الإنتاج.
و`docs/API.md` حُدِّث: أُضيفت الاستقبال والأدوار وكشف الحضور والتقرير اليومي
وطلبات التحديث (٣٠+ مساراً كانت ناقصة)، وصُحِّحت صلاحيات أربعة مسارات.

**٤) ورقة تصوّر الذكاء الاصطناعي** — `docs/AI_STRATEGY.md`. لا شيفرة.

### ما لم يُمَسّ

القائمة تبقى **بالرمز لكل الأدوار بلا استثناء** — حتى حاملي الأسماء. لم يتغيّر
أيٌّ من الأدوار الأحد عشر الأخرى، ولا استثناءات المستخدمين، ولا مسار الاستقبال.
ولا دور فقد شاشةً بالتضييق: كل من ملك `analytics.view` يملك الشاشتين
المستقلّتين، وكل من ملك `report.view` يملك المحادثات وخطة التطوير — يحرسه
اختبار.

### ما يلزم لتشغيله

```bash
php artisan migrate    # يمنح مدير المركز الصلاحية على قاعدة قائمة
```

الهجرة لازمة: منذ صارت الصلاحيات بيانات، لا تكفي المصفوفة على منصّة عاملة.
وهي **إضافة صفٍّ واحد** — لا تمسّ ما حُرِّر من الشاشة، وتتخطّى دوراً جُرِّد
عمداً من كل صلاحياته.

### كيف يُرجَع عنه

```bash
php artisan migrate:rollback --step=1   # يسحب الصلاحية وحدها
git revert <sha>                        # يُعيد البوّابات الأربع
```

أو من شاشة «الأدوار والصلاحيات» بلا نشر: أزل الصلاحية من مدير المركز.

**١٠ اختبارات جديدة**: `CenterManagerNamesTest` (٦)، `PerScreenPermissionGatesTest`
(٤). الحزمة: ٧١٦ ناجحة.

---

## ٢٠٢٦-٠٨-٠٦ — الأدوار والصلاحيات يحرّرها المدير

**الوسم:** `checkpoint/role-editor`

### ما أُضيف

**١) صلاحيات الأدوار صارت بيانات لا شيفرة.** جدول `role_permissions` هو المرجع،
و`Permissions::matrix()` صارت **الافتراضي** الذي يُبذَر به كل دور أول مرّة
ومرجعَ الرجوع لدورٍ لا صفوف له. التغيير يسري فوراً على كل حاملي الدور بلا نشر.

**٢) شاشة «الأدوار والصلاحيات»** (`/roles`, صلاحية `user.manage`): إنشاء دور،
تعديل صلاحياته، إعادته للافتراضي، حذفه. ٥٣ صلاحية في ١٧ مجموعة، لكلٍّ **وصف
عربي**، ويُعرض **الفرق قبل الحفظ** مع عدد المستخدمين المتأثّرين.

**٣) صلاحية مستقلّة لكل شاشة.** كانت خمس صلاحيات تحرس إحدى عشرة شاشة، فلم يكن
يمكن منح شاشةٍ دون أختها. الجديدة: `analytics.executive` و
`analytics.daily_report` و`development_plan.view` و`chat.view` و
`workflow.manage`. المتحكّمات تقبل القديمة أيضاً — إبقاءً على ما كان يعمل.

**٤) مدير المركز يتحكّم لا يشاهد فقط** — من ١٧ صلاحية إلى ٣٧: الجدولة والتوزيع
والاستقبال والاعتماد والقياس والكفاءات. وبلا `user.manage` ولا
`settings.manage` ولا `workflow.manage`: سلطات نظام تبقى لمدير النظام.

### الحرّاس الأربعة

تعديل الدور يمسّ **كل من يحمله**، بخلاف استثناء المستخدم الذي يمسّ واحداً.
ولذلك كانت المصفوفة ثابتة. الفتح مشروط بأربعة لا يُتجاوَز أيٌّ منها:

1. **دور مدير النظام لا يُعدَّل ولا يُحذف** — سحب `user.manage` منه يُغلق باب
   الإدارة على الجميع بلا سبيل للعودة.
2. **لا أحد يعدّل دوره** — وإلا منح نفسه كل شيء.
3. **لا يُمنَح ما لا يملكه المُعدِّل** — سقفٌ لا منعٌ مطلق. يُقيَّد المحاوَلُ في
   التدقيق بـ`DENIED_ROLE_ESCALATION`.
4. **غير القابلة للتفويض لا تُمَسّ من هنا** — تُدار بالدور المبذور.

### تفصيلتان خفيّتان

- **الفراغ المقصود.** تجريد دورٍ من كل صلاحياته كان يحذف صفوفه فيقع على
  المصفوفة **ويستعيد افتراضياته** — أي أنّ السحب الكامل ينقلب إلى استعادةٍ
  صامتة. تمنعه علامة `PLACEHOLDER`، ولا تظهر في أي مخرَج.
- **مفتاح ذاكرة صار صلاحية.** `Permissions::all()` كانت تقرأ ثوابت الصنف
  بالانعكاس وتقبل «كل ما فيه نقطة»، فمرّ مفتاح `kafaat.rolePermissions` وصار
  صلاحيةً وهمية بمربّع اختيار في الشاشة. النمط الآن صارم، ويحرسه اختبار.

### ما لم يُمَسّ

استثناءات المستخدمين الفردية كما هي. وكل دور يعمل بصلاحياته السابقة نفسها —
البذر لا يكتب فوق دورٍ حُرِّرت صلاحياته.

### ما يلزم لتشغيله

```bash
php artisan migrate
php artisan db:seed --class=DatabaseSeeder   # يبذر صلاحيات الأدوار مرّة واحدة
```

### كيف يُرجَع عنه

```bash
git revert <sha>..HEAD
php artisan migrate:rollback --step=1        # يُسقط role_permissions
```
الإسقاط يُعيد المنصّة إلى قراءة المصفوفة الثابتة — بلا فقدان وصول.

**١٧ اختباراً جديداً** في `RolePermissionEditingTest`. الحزمة: ٧٠٦ ناجحة.

---

## ٢٠٢٦-٠٨-٠٦ — إصلاحان ظهرا أثناء النشر على الإنتاج

**الوسم:** `checkpoint/deploy-fixes`

### ١) `--rollback` كان معطّلاً في أداة النشر

`push-release.sh` كان يسرد الإصدارات بـ`ls $RELEASES/*/` بحساب الدخول
(`tamkeenadmin`) الذي **لا يقرأ المجلّد**. فيتوسّع النمط إلى لا شيء، ويُخرج
«Permission denied» إلى stderr، ويُرجع سلسلة فارغة **بلا أن يفشل الأمر**.

الأثر لم يكن تجميلياً:

- `--rollback` يقرأ فراغاً فيقول «لا يوجد إصدار سابق» ويخرج — **الرجوع معطّل**
- ومعه **الرجوع التلقائي عند فشل فحص الصحّة**، وهو الحارس الذي يُفترض أن يعمل
  في أسوأ لحظة بالضبط
- وتنظيف الإصدارات القديمة معطّل كذلك (تراكمت ستّة والحدّ خمسة)

الإصلاح: `releases_desc()` تُوسّع النمط **داخل** `bash` المرفوعة بـ`sudo`.
تحقُّقٌ بعده: الرجوع يحلّ الإصدار السابق، والتنظيف عاد فصارت خمسة.

### ٢) البذر كان يكتب فوق كلمة مرور مدير النظام

`DatabaseSeeder` على الإنتاج كان يمرّ على حساب `admin` بـ`updateOrCreate`.
فإعادةُ تشغيل البذر لسببٍ مرجعيّ بحت — إضافة دور أو قطاع — تُعيد كلمة مرور
مدير النظام إلى `ADMIN_INITIAL_PASSWORD` وتُلزمه بالتغيير. أمرٌ يبدو آمناً
كان ينتزع من صاحب المنصّة كلمةَ مروره التي اختارها، **بلا إنذار**.

الآن: حسابٌ قائم لا يُمَسّ، والإنشاء لأول مرّة فقط. وإعادة الضبط لها أمرها
الصريح `kafaat:create-admin admin --reset`. يحرسه
`tests/Feature/SeederDoesNotOverwriteAdminTest.php`.

> لهذا **لم يُشغَّل البذر الشامل** على خادم الإنتاج عند نشر الاستقبال؛ أُدرج
> دور مسؤول العمليات جراحياً وحده بعد التحقّق أن كلمة مرور `admin` لم تتغيّر.

---

## ٢٠٢٦-٠٨-٠٦ — دور مسؤول العمليات وأول حساب له

**الوسم:** `checkpoint/operations-role`

- أُنشئ حساب **`aalqabbani` — عبدالله القباني** بدور «مسؤول العمليات».
  يعمل بالرمز: لا يرى أسماء المشاركين ولا سِيَرهم، ويرى «استقبال الموظفين»
  و«المشاركون» و«الجدولة» و«الحضور» فقط.
- أُعيدت تسمية ملفَّي التوثيق: قواعد العمل صارت `AGENTS.md`، وسجلّ التغييرات
  صار باسم المنصّة `منصة-تمكين-الكفاءات.md`.

أُنشئ على بيئة التطوير، ثم على **خادم الإنتاج** (الإصدار `20260806152121`)
بعد التحقّق من الدخول والصلاحيات بمتصفّح حقيقي.

---

## ٢٠٢٦-٠٨-٠٦ — استقبال الموظفين

**الوسم:** `checkpoint/reception-desk`

### ما أُضيف

مسار كامل للمشارك من باب المركز إلى جدول المقابلات، بست مراحل لكلٍّ صلاحيتها:

| # | المرحلة | الصلاحية | صاحبها |
|---|---|---|---|
| ١ | تسجيل الوصول (الوقت تلقائي وقابل للتعديل) | `reception.record` | مسؤول استقبال الموظفين |
| ٢ | التوقيع والإقرار بصحّة البيانات | `reception.record` | مسؤول استقبال الموظفين |
| ٣ | التوزيع على مقابلة/حلقة نقاش/أدوات قياس | `reception.assign` | الاستقبال والعمليات |
| ٤ | استلام المشارك أو ردّه بسبب | `reception.decide` | المقيّم المُسنَد إليه |
| ٥ | إعادة إسناد المردود | `reception.assign` | مسؤول العمليات |
| ٦ | الاعتماد وترحيل الجلسات إلى الجدول | `reception.approve` | مسؤول العمليات |

وفتحُ الشاشة نفسها يلزمه `reception.view`.

- **جدولان جديدان**: `reception_visits` (الوصول والتوقيع والإقرار)
  و`reception_assignments` (الإسناد وقرار المقيّم). لا تعديل على عمودٍ قائم.
- **دور جديد**: `OPERATIONS` «مسؤول العمليات». يعمل بالرمز: بلا رؤية أسماء
  ولا سِيَر — قراره إجرائي (مَن يقابل مَن) لا محتوائي.
- **`ReceptionController`** و`app/Models/Reception*.php` و`views/Reception.vue`
  و`components/SignaturePad.vue`.
- **`NotificationService::notifyPermission()`** — إشعار بحسب الصلاحية لا برمز
  الدور، كي لا يذهب إشعار الردّ إلى لا أحد في مركزٍ لم يُنشئ دور العمليات.
- **٢٤ اختباراً** في `tests/Feature/ReceptionFlowTest.php`.

### الضوابط المقصودة

- **المقيّم لا يرى اسم المشارك ولا رقم هويته في أي مخرَج من هذا المسار** — لا
  بشرط صلاحية ولا بدونه. قاعدة إجرائية لا قاعدة صلاحية: حتى مَن يملك
  `candidate.view_names` لا يراه هنا (يثبته اختبار مستقل).
- التوقيع يُخزَّن **مشفَّراً** (`Crypt`) ولا يُكتب في سجلّ التدقيق.
- لا توزيع قبل التوقيع والإقرار؛ ولا اعتماد قبل بتّ كل إسناد معلّق؛ والزيارة
  المعتمدة **مجمَّدة** (لا يُعدَّل وقتها ولا يُستبدل توقيعها).
- إسنادٌ فعّال واحد لكل (زيارة، نشاط) — بفهرس فريد جزئي في Postgres.
- الإسناد المردود **يبقى في السجلّ** بسببه؛ إعادة الإسناد صفٌّ جديد.

### ما لم يُمَسّ

الجدولة والحضور والتقييم والتقارير كلها كما هي. الاعتماد يُنشئ صفوفاً في
`schedules` فتلتقطها الشاشات القائمة بلا تعديل فيها.

### ما يلزم لتشغيله

```bash
php artisan migrate                       # ينشئ الجدولين
php artisan db:seed --class=DatabaseSeeder  # يضيف دور OPERATIONS
```

ثم يُنشأ مستخدم بدور «مسؤول العمليات» من شاشة المستخدمين. **بدونه** يبقى المسار
عاملاً حتى المرحلة الرابعة ثم يقف المردود بلا من يعيد إسناده — ويُسجَّل ذلك في
التدقيق بـ`RECEPTION_REJECT_UNROUTED`.

### كيف يُرجَع عنه

```bash
git revert <sha>..HEAD                    # تراجع يُبقي التاريخ
php artisan migrate:rollback --step=1     # يُسقط الجدولين (وما فيهما)
```

الرابط وحده يُخفى بـ`FEATURES` في الواجهة دون مسّ البيانات.

---

## ٢٠٢٦-٠٨-٠٦ — تعديلات الواجهة المصاحبة

**في مستودع `frontend`** — راجع `frontend/منصة-تمكين-الكفاءات.md`.

- اللوحة التنفيذية صارت باباً مستقلاً في القائمة الجانبية (كانت رابطاً داخل
  التحليلات).
- «التقرير اليومي»: تُوسِّط الصفحة وضُبطت مقاسات البطاقات والجداول.
- «طلبات تحديث البيانات» مُخفاة مؤقتاً بمفتاح تشغيل — الشيفرة والمسارات
  والاختبارات كلها قائمة.

---

## ما قبل ذلك

لم يكن هذا السجلّ موجوداً قبل ٢٠٢٦-٠٨-٠٦. تاريخ ما سبق في وسوم الرجوع:

```bash
git tag -l 'checkpoint/*' -n1
```
