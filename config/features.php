<?php

// ════════════════════════════════════════════════════════════
//  مفاتيح تشغيل الخدمات على الخادم.
//
//  نظير frontend/src/services/features.js — لكنّ ذاك إخفاءُ شاشة، وهذا
//  إغلاقُ سطح API. إخفاء الشاشة وحده لا يكفي لخدمةٍ عامة بلا مصادقة:
//  مساراتها تبقى مفتوحة لمن يعرف عنوانها، والرابط يبقى يُرسَل في الرسائل.
//
//  env() داخل ملف إعدادات آمن مع config:cache (يُخبَز وقت البناء).
// ════════════════════════════════════════════════════════════

return [

    // بوّابة المرشّح العامة: /confirm/{token} ومساراتها /api/public/assessment/*.
    // مُعطَّلة بطلب صاحب المنصّة حتى إشعار آخر — سيُعاد بناؤها لاحقاً.
    // التعطيل يُسقط ثلاثة أشياء معاً كي لا يبقى نصفُ بوّابةٍ حيّاً:
    //   • مسارات الـAPI الأربعة (verify/confirm/arrive/cv)
    //   • رابط التأكيد داخل رسالة SMS التي تُرسَل عند إضافة كل مرشّح
    //   • مسار /confirm في موجّه الواجهة
    // لإعادتها: CANDIDATE_PORTAL_ENABLED=true في .env، وfeatures.js في الواجهة.
    'candidate_portal' => (bool) env('CANDIDATE_PORTAL_ENABLED', false),

    // كشك الاستقبال على الجهاز اللوحي: /kiosk/{token} ومسارات /api/kiosk/*.
    // شبكةٌ داخلية لا DMZ — ومع ذلك يُعامَل الرمز كغير موثوق: بوّابة هوية،
    // وتقييد معدّل، ونطاق يومٍ واحد قابل للإبطال.
    'reception_kiosk' => (bool) env('RECEPTION_KIOSK_ENABLED', true),

];
