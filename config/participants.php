<?php

// ════════════════════════════════════════════════════════════
//  ما هو إلزاميّ في بيانات المشارك — مفتاحٌ واحد يُقرأ من كل باب.
//
//  كانت القاعدة منثورةً في ستّة مواضع: نموذج الإضافة، والتعديل، والمستورِد،
//  وبوّابة الترشيح، وبوّابة الإقرار، ومحقّق السيرة. فتشديدُ حقلٍ يتطلّب تذكّر
//  مواضعه كلّها، ونسيانُ موضعٍ يعني باباً يقبل ما ترفضه الأبواب الخمسة.
//
//  القرار القائم: أربعةٌ إلزامية وحدها — الهوية والاسم والقطاع والرتبة. وهي
//  الأربعة نفسها التي تفرضها القاعدة NOT NULL، فلا يوجد إلزامٌ في التحقّق بلا
//  سندٍ في المخطّط. وما عداها اختياريّ: يُتحقَّق من صيغته إن كُتب، ولا يُطلب.
//
//  ولإعادة حقلٍ إلزاميّاً: انقله من `optional` إلى `required` هنا، وأضِف
//  نظيره في frontend/src/services/fieldRules.js (المرآة على العميل). لا موضع
//  ثالث.
// ════════════════════════════════════════════════════════════

return [

    // الحقول التي لا يُحفظ مشاركٌ بدونها. مطابِقةٌ لأعمدة NOT NULL في `candidates`.
    'required' => [
        'nationalId',
        'fullName',
        'sectorId',
        'rankLabel',
    ],

    // اختياريّة اليوم — تُتحقَّق صيغتها متى كُتبت، ولا تُطلب.
    // القائمة مذكورة صراحةً لا استنتاجاً: قارئُ الملفّ يريد أن يرى ما رُفع عنه
    // الإلزام، لا أن يستنتجه من غياب اسمٍ عن القائمة الأولى.
    'optional' => [
        'gender',
        'mobile',
        'email',
        'militaryNumber',
        'personnelCategory',
        'tier',
        'assessmentType',
        'classification',
        'technicalAreaIds',
        'notes',
        'cv',
    ],

    // ── قِيَمٌ افتراضية لما رُفع عنه الإلزام ──
    // عمود `personnel_category` في القاعدة NOT NULL DEFAULT 'civilian'. رفعُ
    // الإلزام لا يرفع القيد، فمن لم يُرسل فئةً يأخذ هذه — ولا يُسند null أبداً.
    'defaults' => [
        'personnelCategory' => 'civilian',
        'tier' => 'middle',
    ],

];
