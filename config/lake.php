<?php

// ════════════════════════════════════════════════════════════════════════
//  بحيرة تقارير كفاءات — الإعدادات
//
//  كل إعداد يُقرأ عبر config('lake.…') لا عبر env() مباشرةً في الشيفرة:
//  deploy.sh ينفّذ config:cache، وبعده تُرجع env() خارج config/ قيمتَها
//  الافتراضية دائماً. هذا الملف هو الموضع الوحيد الذي يُسمح فيه بـ env().
//
//  الوضع الافتراضي: مُعطّل بالكامل. تفعيلُه قرارٌ صريح في .env ثم إعادة نشر.
// ════════════════════════════════════════════════════════════════════════

return [

    // المفتاح الرئيس: ما لم يكن true فالمُصدِّر لا يفعل شيئاً إطلاقاً —
    // لا كتابة في صندوق الصادر، ولا اتصال بالبحيرة، ولا كلفة على الطلب.
    'enabled' => env('LAKE_ENABLED', false),

    // فلفل HMAC لسكّ المعرّف البديل person_ref.
    // لا يغادر خادم التطبيق أبداً، ولا يُكتب في البحيرة، ولا يُشتقّ من APP_KEY
    // (فقدان APP_KEY يُفقد الـPII المشفّرة؛ وفقدان هذا يُفقد الربط عبر الدورات فقط).
    // تغييرُه يقطع استمرارية الشخص عبر الأحداث السابقة — يُغيَّر بقرار، لا بالخطأ.
    'pepper' => env('LAKE_PEPPER'),

    // فضاء أسماء UUIDv5 — يجعل معرّف الحدث اشتقاقياً لا عشوائياً،
    // فإعادةُ تشغيل التعبئة التاريخية لا تُضاعف الصفوف (ON CONFLICT يمتصّها).
    'uuid_namespace' => env('LAKE_UUID_NAMESPACE', '6f9c4a1e-3d52-4b8a-9e17-2c5b8d0a7f34'),

    // ── ما يُنشر فعلياً ────────────────────────────────────────────────
    // اختار المالك «مجهول الهوية بالكامل — معرّف بديل فقط: أرقام ومؤشرات
    // وقطاعات». هذه المفاتيح تُجسّد ذلك القرار، وكلٌّ منها يُفتح على حدة
    // بمراجعةٍ مكتوبة لا بتبديل سطر.
    'publish' => [
        // النصّ الحرّ للتقرير (نظرة عامة، ملخّص تنفيذي، نقاط القوة، مجالات التطوير).
        // مُعطّل: نصّ التقرير أعلى مخاطر إعادة التعرّف رغم التنقية.
        'narrative' => env('LAKE_PUBLISH_NARRATIVE', false),

        // رمز المشارك (مثل «أمن-014»). ليس اسماً، لكنه يكشف القطاع وترتيب
        // الالتحاق ويربط صفّ البحيرة بشاشة المنصّة مباشرةً. مُعطّل افتراضياً؛
        // يُفعَّل لو احتاج الفريق مطابقةً يدويةً بين النظامين.
        'participant_code' => env('LAKE_PUBLISH_PARTICIPANT_CODE', false),

        // المستندات المُصيَّرة (HTML). مُعطّل: تحمل النصّ الحرّ كاملاً،
        // وتصييرها داخل الطلب يُضيف كلفةً على مسار الاعتماد بلا مقابل الآن.
        'documents' => env('LAKE_PUBLISH_DOCUMENTS', false),
    ],

    // التصنيفات المسموح لها بمغادرة القاعدة الأساسية.
    // لا يُوسَّع هذا إلا بموافقة أمن المعلومات: المنصّة تُنكر وجود الصفّ
    // المُصنَّف أصلاً (404 لا 403)، فتسريبُه إلى البحيرة نقضٌ للضابط نفسه.
    'classifications' => ['normal'],

    // ── التشغيل ────────────────────────────────────────────────────────
    'batch_size' => (int) env('LAKE_BATCH_SIZE', 500),

    // بعدها يُنقل صفّ الصادر إلى الحجر الصحّي بدل أن يُعطّل الدفعة إلى الأبد.
    'max_attempts' => (int) env('LAKE_MAX_ATTEMPTS', 8),

    // تراكمٌ فوق هذا الحدّ يعني أن البحيرة متوقّفة؛ يُسجَّل تنبيهٌ واحد
    // لا تنبيهٌ لكل صفّ.
    'backlog_alarm' => (int) env('LAKE_BACKLOG_ALARM', 5000),

    // عدد الأشهر التي تُهيَّأ أقسامُها مُقدَّماً في raw.report_events.
    'partition_months_ahead' => (int) env('LAKE_PARTITION_MONTHS_AHEAD', 15),

    // حدُّ الإخفاء الإحصائي لا يُضبط من هنا: العروضُ تقرؤه من
    // meta.lake_settings داخل البحيرة نفسها (lake.k_anonymity()). مفتاحُ
    // بيئةٍ هنا كان سيُوهم أنه يُغيّر شيئاً وهو لا يصل SQL أصلاً.
    //   UPDATE meta.lake_settings SET value='7' WHERE key='k_anonymity';

    // الاتصال المستخدَم للكتابة (lake_writer) وللهجرات (lake_owner).
    'connection' => env('LAKE_CONNECTION', 'pgsql_lake'),
    'ddl_connection' => env('LAKE_DDL_CONNECTION', 'pgsql_lake_ddl'),

    // إصدار العقد المنشور — يُختم على كل حدث فيَعرف المستهلك ما يقرأ.
    'contract_version' => 'report.v1',
];
