# منصة مركز تمكين الكفاءات — قواعد العمل الثابتة

هذا الملف يُقرأ في كل جلسة. القواعد أدناه ليست اقتراحات.

---

## ١) قاعدة النشر: كل خدمة إضافة، ولكل إضافة نقطة رجوع ونسخة احتياطية وسطر في السجلّ

**لا يُعدَّل سلوكٌ قائم ليُستبدل بآخر.** الخدمة الجديدة تُضاف بجانب القائم،
ويُترك القائم عاملاً حتى تُقبل الجديدة. سببها أنّ الرجوع عن استبدالٍ يعني إعادة
كتابة، أمّا الرجوع عن إضافةٍ فيعني وسمَ رجوع وأمرَ `git checkout` واحداً.

وإخفاء شاشة قائمة يكون **بمفتاح تشغيل** في `frontend/src/services/features.js`
لا بحذف شيفرتها — فإعادتها سطرٌ واحد لا إعادةُ كتابة.

**بعد كل إضافة مكتملة — قبل الانتقال إلى غيرها، أربعة أشياء لا ثلاثة:**

```bash
# ١) نسخة احتياطية لقاعدة البيانات قبل أي هجرة أو نشر
./backup-db.sh                          # في جذر المستودع الأعلى

# ٢) سطر في سجلّ التغييرات يقول ما الذي أُضيف
$EDITOR CHANGELOG.md

# ٣) لقطة التزام
git add -A && git commit -m "<وصف الإضافة>"

# ٤) نقطة رجوع موسومة
git tag -a checkpoint/<اسم-مختصر> -m "<ما الذي يعمل عند هذه النقطة>"
git push origin Dev --tags
```

**سجلّ التغييرات ([`CHANGELOG.md`](CHANGELOG.md)) إلزامي.** لكل إضافة مدخل يذكر:
ما أُضيف، وما لم يُمَسّ، وأي هجرة تلزم، وأي وسم يقابلها، وكيف يُرجَع عنها.
غايته أن يُجيب بعد شهور سؤالاً واحداً: «ما الذي دخل، ومتى، وكيف أخرجه؟»

قواعد الوسم:
- الاسم بصيغة `checkpoint/<موضوع>` بالإنجليزية المختصرة (`checkpoint/login-cleanup`).
- الرسالة تصف **ما الذي يعمل** عند النقطة، لا ما تغيّر — لأنها تُقرأ وقت الرجوع
  حين يكون السؤال «إلى أين أرجع كي يعمل كذا».
- الوسم بعد التحقّق لا قبله: نقطةٌ لم تُختبر ليست نقطةَ رجوع بل فخّ.

**الرجوع:**
```bash
git tag -l 'checkpoint/*' -n1          # ما النقاط المتاحة وما يعمل عند كلٍّ منها
git diff checkpoint/<اسم> --stat        # ما الذي تغيّر منذئذ
git revert <sha>..HEAD                  # تراجُعٌ يُبقي التاريخ (المفضَّل)
git checkout checkpoint/<اسم> -- <مسار> # استرجاع ملف بعينه
```
لا `git reset --hard` على فرعٍ مدفوع.

**قبل أي مسحٍ للبيانات:** نسخة احتياطية أولاً، دائماً. الأمر `platform:reset`
يأخذها بنفسه ويتوقّف إن فشلت.

---

## ٢) قاعدة التوثيق: الدليل جزء من الخدمة لا ملحقٌ بها

**الخدمة لا تُعدّ مكتملة قبل تحديث دليل المستخدم.** الدليل في
[`docs/USER_GUIDE.md`](docs/USER_GUIDE.md) وصوره في `docs/guide-images/`.

بعد كل خدمة جديدة أو تغييرٍ في شاشة قائمة:

```bash
node docs/capture-guide.mjs        # يلتقط صور الشاشات من بيئة التطوير
```
ثم يُحرَّر القسم المقابل في `USER_GUIDE.md` نصّاً.

الدليل يوثّق **ما يراه المستخدم ويفعله**، لا كيف بُني. سطر واحد لكل حقل مهم،
وصورة لكل شاشة، وذكرٌ صريح لمن يملك الصلاحية.

المسار الذهبي: كل شاشة في الدليل تُجيب ثلاثة أسئلة — مَن يفتحها، وما الذي
يفعله فيها، وما الذي يمنعه النظام ولماذا.

---

## ٣) الفروع

`Dev` هو فرع العمل دائماً. `Staging` و`Production` لا يُدفَع إليهما إلا بطلبٍ
صريح من صاحب المنصّة.

---

## ٤) بيانات الدخول

لا تُكتَب بيانات دخول في الواجهة ولا في الشيفرة ولا في التوثيق — لا كقيمة
افتراضية في حقل، ولا كسطر «حساب تجريبي». حساب المدير الأول يُنشأ من
`ADMIN_INITIAL_PASSWORD` مع `must_change_password = true`.

---

## ٥) البيئة

- PHP **8.4+** إلزاماً (`composer.lock` يثبّت `symfony/http-foundation` الذي يشترط ≥8.4.1)
- PostgreSQL، وRedis للجلسة والذاكرة والطابور — لا `sync` في الإنتاج
- بعد `config:cache` لا تُقرأ `.env` إطلاقاً: كل إعداد يلزم وقت التشغيل يُقرأ
  عبر `config('...')` من ملف إعدادات، لا عبر `env()` خارجها
